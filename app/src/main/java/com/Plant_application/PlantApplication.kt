package com.Plant_application

import android.app.Application

class PlantApplication : Application() {
    override fun onCreate() {
        super.onCreate()
        // 앱 전체에서 필요한 초기화 코드를 여기에 추가할 수 있습니다.
    }
}