<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    tools:context=".ui.search.SearchFragment">

    <!-- SearchView -->
    <androidx.appcompat.widget.SearchView
        android:id="@+id/search_view"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_margin="16dp"
        app:queryHint="식물 별명으로 검색"
        app:iconifiedByDefault="false"
        android:background="@drawable/bg_search_background" />

    <!-- Beige background with rounded top corners -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:orientation="vertical"
        android:background="@drawable/bg_second_background">

        <TextView
            android:id="@+id/tv_recommend_title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="오늘의 추천 식물"
            android:textColor="@color/text_primary"
            android:textSize="18sp"
            android:padding="18dp"
            android:paddingStart="20dp" />

        <!-- ⭐ 원형 + 오른쪽 박스를 가로로 배치 -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:paddingStart="20dp"
            android:paddingEnd="20dp"
            android:gravity="center_vertical">

            <!-- 원형 이미지 -->
            <FrameLayout
                android:id="@+id/plant_image_container"
                android:layout_width="130dp"
                android:layout_height="130dp"
                android:background="@drawable/bg_green_circle"
                android:clipToOutline="true">

                <ImageView
                    android:id="@+id/img_recommended_plant"
                    android:layout_width="match_parent"
                    android:layout_height="match_parent"
                    android:scaleType="centerCrop"
                    android:visibility="gone" />

                <TextView
                    android:id="@+id/tv_placeholder"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="식물 이미지"
                    android:textColor="@color/text_primary"
                    android:textSize="16sp"
                    android:gravity="center"
                    android:layout_gravity="center" />

            </FrameLayout>

            <!-- ⭐ 오른쪽 박스 -->
            <LinearLayout
                android:id="@+id/right_box"
                android:layout_width="0dp"
                android:layout_height="130dp"
                android:layout_weight="1"
                android:orientation="vertical"
                android:layout_marginStart="20dp"
                android:background="@color/gray">

                <TextView
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="추천 식물 정보"
                    android:textSize="16sp"
                    android:textColor="@color/text_primary"/>
            </LinearLayout>

        </LinearLayout>

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:paddingTop="16dp"
            android:paddingStart="20dp"
            android:paddingEnd="20dp"
            android:gravity="center_vertical">

            <!-- 왼쪽 텍스트 -->
            <TextView
                android:id="@+id/tv_add_plant"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:text="이 식물을 추가하시겠어요?"
                android:textColor="@color/text_primary"
                android:textSize="16sp" />

            <!-- 오른쪽 끝 + 버튼 -->
            <ImageView
                android:id="@+id/btn_add_plant"
                android:layout_width="32dp"
                android:layout_height="32dp"
                android:src="@drawable/plus2"
                android:contentDescription="식물 추가 버튼"/>
        </LinearLayout>

        <!-- ⭐ 회원님을 위한 추천 식물 타이틀 -->
        <TextView
            android:id="@+id/tv_user_recommend_title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="회원님을 위한 추천 식물"
            android:textColor="@color/text_primary"
            android:textSize="18sp"
            android:padding="18dp"
            android:paddingStart="20dp" />

        <!-- ⭐ 회원 추천 박스 (회색 박스) -->
        <LinearLayout
            android:id="@+id/user_recommend_box"
            android:layout_width="match_parent"
            android:layout_height="100dp"
            android:orientation="vertical"
            android:background="@color/gray"
            android:layout_marginStart="20dp"
            android:layout_marginEnd="20dp"
            android:layout_marginBottom="12dp"
            android:padding="16dp">

            <TextView
                android:id="@+id/tv_user_recommend_placeholder"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="추천 식물 정보를 불러오는 중..."
                android:textColor="@color/text_primary"
                android:textSize="14sp" />

        </LinearLayout>

        <TextView
            android:id="@+id/tv_no_results"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="검색 결과가 없습니다."
            android:visibility="gone" />

        <!-- RecyclerView 아래는 그대로 유지 -->
        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/rv_search_results"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1"
            android:paddingHorizontal="16dp"
            app:layoutManager="androidx.recyclerview.widget.LinearLayoutManager"
            tools:listitem="@layout/item_plant" />

    </LinearLayout>

</LinearLayout>
